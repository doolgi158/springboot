<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boot.example.mapper.BookMapper">
    <resultMap id="bookResult" type="com.boot.example.domain.Book">
        <id column="book_id" property="bookId" />
        <result column="title" property="title" />
        <result column="author" property="author" />
        <result column="publisher" property="publisher" />
        <result column="publish_date" property="publishDate" />
        <result column="price" property="price" />
    </resultMap>

    <select id="bookList" resultMap="bookResult">
        SELECT BOOK_ID, TITLE, AUTHOR, PUBLISHER, PUBLISH_DATE, PRICE
        FROM BOOK
        ORDER BY BOOK_ID DESC
    </select>

    <insert id="bookInsert" parameterType="com.boot.example.domain.Book">
        INSERT INTO BOOK (TITLE, AUTHOR, PUBLISHER, PUBLISH_DATE, PRICE)
        VALUES (#{title}, #{author}, #{publisher}, #{publishDate}, #{price})
    </insert>

    <delete id="bookDelete" parameterType="com.boot.example.domain.Book">
        DELETE FROM BOOK WHERE BOOK_ID = #{bookId}
    </delete>

    <update id="bookUpdate" parameterType="com.boot.example.domain.Book">
        UPDATE BOOK SET TITLE = #{title}, AUTHOR = #{author}, PUBLISHER = #{publisher}, PUBLISH_DATE = #{publishDate}, PRICE = #{price}
        WHERE BOOK_ID = #{bookId}
    </update>
</mapper>